<catel:DataWindow x:Class="Orc.Search.Example.Views.AddPersonWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:catel="http://schemas.catelproject.com"
                  xmlns:orccontrols="http://schemas.wildgums.com/orc/controls"
                  WindowStyle="ToolWindow">

    <orccontrols:StackGrid>
        <orccontrols:StackGrid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </orccontrols:StackGrid.RowDefinitions>

        <orccontrols:StackGrid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </orccontrols:StackGrid.ColumnDefinitions>

        
        <TextBlock Text="First name"/>
        <TextBox Text="{Binding Person.FirstName}"/>
            
        <TextBlock Text="Second name"/>
        <TextBox Text="{Binding Person.LastName}"/>

        <TextBlock Text="Age"/>
        <TextBox Text="{Binding Person.Age}"/>

    </orccontrols:StackGrid>
    
</catel:DataWindow>







